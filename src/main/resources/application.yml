spring:
  application:
    name: CLIENT-SERVICE
server:
  port: ${PORT:8081}
eureka:
  instance:
    instance-id: ${spring.application.name}-${SEQUENCE_NO}
  client:
    service-url:
      default-zone: http://admin:admin123@localhost:8761/

#spring:
#  cloud:
#    inetutils:
#      ignored-interfaces:
#        - eth1*

#eureka:
#  client:
#    registry-fetch-interval-seconds: 3
#    disable-delta: true

